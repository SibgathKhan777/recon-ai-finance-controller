# Optional. If set, exception explanations are generated live via Claude.
# If unset, the pipeline falls back to rule-based template explanations —
# it is fully functional either way.
ANTHROPIC_API_KEY=
